# Runtime dependencies (phases 0-4).
# Later phases will add: paddleocr (OCR — optional, Phase 6).
PyQt6>=6.7.0
ijson>=3.2.0
platformdirs>=4.0.0
numpy>=2.0
ollama>=0.4
google-genai>=1.0
# Phase 4 — CLIP for visual search.
Pillow>=10.0
# Install torch/torchvision separately; on Windows w/ NVIDIA:
#   pip install torch --index-url https://download.pytorch.org/whl/cu129
#   pip install "torchvision==0.24.0" --no-deps
open-clip-torch>=3.0
# Phase 4.5 — doc ingest (PDF → PNG + OCR).
pypdfium2>=4.0
pdfplumber>=0.11
easyocr>=1.7
# Phase 7 — web source ingest (Linnsoft forum and similar).
beautifulsoup4>=4.12
requests>=2.31
# Shippable data-pack (tar.zst). Pure-Python bindings to libzstd so no
# system install is needed on Windows. Used by packaging/build_data_pack.py
# and the in-app File → Install data pack… flow.
zstandard>=0.22
